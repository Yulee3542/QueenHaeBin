# ROS 2 전환 후 대부분의 의존성(opencv/numpy/pyserial/matplotlib)은 rosdep이
# autodrive_skku_ros/package.xml을 보고 apt로 설치한다 (setup.sh/update.sh 참고).
#
# rplidar-roboticia만 예외 — apt/rosdep에 대응하는 패키지가 없는 PyPI 전용
# 패키지인데, vendor/Function_Library.py(SKKU 제공, 수정 금지)가 최상단에서
# `from rplidar import RPLidar`를 무조건 실행한다. 우리 코드는 이제 이 클래스를
# 쓰지 않지만(라이다 통신은 공식 rplidar_ros ROS 드라이버가 담당), 그래도 임포트가
# 성공해야 Function_Library의 다른 함수(edge_detection 등)를 쓸 수 있다.
#
# 설치: pip3 install --user -r requirements.txt
rplidar-roboticia

# ultralytics(+torch, 자동 설치) — traffic 미션의 YOLO 신호등 색상 인식(1순위)
# 용. 완전히 선택 사항: 미설치면 missions/traffic_yolo.py의 load_model()이
# None을 반환하고 traffic.py가 자동으로 HSV 검출(detect_light_color)만 쓴다
# (예외 없음). 무거운 의존성(torch)이라 개발/CI 환경에서는 생략해도 무방 —
# tools/smoke_test_missions.py는 두 경우 모두 통과해야 한다.
ultralytics
